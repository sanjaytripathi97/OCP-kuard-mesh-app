apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: kuard-gw
  namespace: argocd-sanjay-test
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "argo-sanjay-test.apps.apsara.lab.psi.pnq2.redhat.com"
